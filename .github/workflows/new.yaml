name: Slack Notify

on:
  push:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send Slack
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"Hello, World!!"}' \
          ${{ secrets.SLACK_TEST }}